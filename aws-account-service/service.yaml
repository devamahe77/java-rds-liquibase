apiVersion: v1
kind: Service
metadata:
  name: aws-account-service
spec:
  selector:
    app: aws-account-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
